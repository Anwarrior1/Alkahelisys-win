# قائمة التحقق النهائية على Windows

استخدم جهاز Windows x64 نظيفاً لا يحتوي على نسخة سابقة من النظام. استخدم **المثبت الذي بُني من المصدر الحالي** باسم `Alkahelisys-Windows-x64-Setup.exe`. أنشئ بيانات اختبار واضحة تبدأ بـ `WIN-TEST-` ولا تستخدم بيانات عمل حقيقية.

في كل خطوة، سجّل النتيجة **PASS** أو **FAIL** مع لقطة شاشة عند الفشل.

1. **ثبّت ملف NSIS.** شغّل `Alkahelisys-Windows-x64-Setup.exe` وأكمل التثبيت للمستخدم الحالي.
   - **PASS:** ينتهي التثبيت بلا رسالة خطأ ويظهر التطبيق في قائمة Start باسم مركز الكحيلي لغسيل السيارات.
   - **FAIL:** يتوقف التثبيت، أو يطلب ملفات/بيئة تطوير، أو لا يظهر التطبيق.

2. **شغّل التطبيق من Windows.** افتحه من قائمة Start (أو اختصار سطح المكتب إن ظهر).
   - **PASS:** تفتح نافذة ERP العربية بصورة طبيعية.
   - **FAIL:** لا تفتح، تتعطل، أو تظهر صفحة/رسالة خطأ بدلاً من التطبيق.

3. **تحقق من التشغيل المستقل.** لا تفتح Terminal أو Command Prompt ولا تشغّل Node أو Rust أو أي خادم يدوي.
   - **PASS:** تعمل شاشة الدخول والصفحات من التطبيق وحده.
   - **FAIL:** يحتاج التطبيق إلى أمر تطوير أو إلى خادم `localhost` خارجي.

4. **ادخل كمدير.** أنشئ حساب المدير الأول إن ظهرت شاشة الإعداد، أو استخدم حساب المدير المخصص للاختبار.
   - **PASS:** تدخل لوحة المدير وتظهر الأقسام الإدارية والمالية.
   - **FAIL:** لا يمكن الدخول، أو تظهر صلاحيات موظف بدلاً من صلاحيات المدير.

5. **أنشئ بيانات اختبار معرّفة.** أضف عاملًا مثل `WIN-TEST-عامل`، معرضاً مثل `WIN-TEST-معرض`، ومستخدم موظف مثل `win.test.employee` بكلمة مرور اختبارية. سجّل أرقام اللوحات/الملاحظات ببادئة `WIN-TEST-`.
   - **PASS:** تظهر البيانات مرة واحدة في القوائم الصحيحة بعد الحفظ.
   - **FAIL:** لا تحفظ، تتكرر دون سبب، أو تظهر في قسم غير صحيح.

6. **أغلق التطبيق بالكامل.** أغلق نافذة ERP وانتظر بضع ثوانٍ؛ لا تتركه مفتوحاً في شريط المهام.
   - **PASS:** تغلق النافذة بلا رسالة خطأ.
   - **FAIL:** تبقى نافذة أو عملية ظاهرة، أو تظهر رسالة تعطل.

7. **أعد الفتح وتحقق من الحفظ.** افتح ERP وسجّل دخول المدير مرة أخرى.
   - **PASS:** تبقى كل بيانات `WIN-TEST-` التي أُنشئت في الخطوة 5 موجودة.
   - **FAIL:** تفقد البيانات، أو يعود التطبيق إلى إعداد أول تشغيل.

8. **أعد تشغيل Windows.** اختر Restart من Windows، ثم افتح ERP بعد عودة النظام.
   - **PASS:** يعمل ERP وتبقى بيانات `WIN-TEST-` نفسها بعد تسجيل الدخول.
   - **FAIL:** يفشل التشغيل أو تختفي/تتغير البيانات.

9. **تحقق من الحفظ بعد إعادة التشغيل.** افتح العامل والمعرض والمستخدم الاختباريين.
   - **PASS:** الأسماء والتفاصيل التي أدخلتها ما زالت مطابقة.
   - **FAIL:** أي بيانات اختبار مفقودة أو مستبدلة أو مكررة.

10. **اختبر حساب الموظف.** سجّل خروج المدير ثم ادخل باسم `win.test.employee`.
    - **PASS:** يفتح حساب الموظف ويظهر فقط ما تسمح به صلاحياته.
    - **FAIL:** لا يمكن الدخول بسبب بيانات صحيحة، أو يدخل كمدير.

11. **تحقق من الصلاحيات وعزل البيانات.** كموظف، حاول فتح الأقسام المالية/الإعدادات/النسخ الاحتياطي، وراجع العمليات المعروضة. ارجع للمدير وعدّل صلاحية الموظف من المستخدمين إن لزم، ثم أعد الدخول واختبر أن التغيير يسري.
    - **PASS:** الأقسام غير المسموحة مخفية أو ترفض الوصول، ولا تظهر عمولات أو أرباح أو بيانات غير تابعة للموظف؛ تتغير الصلاحية الممنوحة فقط.
    - **FAIL:** يصل الموظف إلى بيانات مالية/إدارية محظورة أو إلى عمليات موظفين آخرين دون صلاحية.

12. **سجّل دورة غسيل واقعية.** كمدير، أنشئ عملية غسيل نقدية للسيارة `WIN-TEST-01` مرتبطة بالعامل الاختباري، بسعر واضح مثل `100.000`.
    - **PASS:** تظهر العملية مرة واحدة في عمليات الغسيل وملف العامل، وتنعكس في الإجماليات ذات الصلة.
    - **FAIL:** لا تظهر، تظهر مرتين، أو لا تتطابق قيمتها/عاملها.

13. **اختبر السيارة الخالصة.** اعتمد عملية `WIN-TEST-01` كـ «خالصة» من زر علامة الصح.
    - **PASS:** تنتقل العملية إلى «السيارات الخالصة» مع الاحتفاظ بالسجل الأصلي ودون إنشاء غسلة ثانية.
    - **FAIL:** لا تنتقل، تتكرر، أو يتغير تاريخها/عاملها/سعرها بلا سبب.

14. **اختبر سيارة المبيت.** أنشئ أو عدّل عملية اختبار ثانية `WIN-TEST-02` وفعّل «تعليم كسيارة مبيتة»، ثم أكملها حسب زر الإجراء الظاهر في سجل سيارات المبيت.
    - **PASS:** تظهر مرة واحدة في «سيارات المبيت»، ثم تخرج منه عند الإكمال مع بقاء عملية الغسيل وتاريخها الأصليين.
    - **FAIL:** تختفي العملية الأصلية، تتكرر، أو تتغير آثارها المالية بصورة غير منطقية.

15. **اختبر حركة مالية لعامل.** من ملف `WIN-TEST-عامل` افتح «المسحوبات والمرتجعات» وأضف «مسحوب» صغيراً، مثلاً `5.000`، مع ملاحظة `WIN-TEST`.
    - **PASS:** تظهر الحركة مرة واحدة ويتغير الرصيد القائم بالمقدار المتوقع؛ تبقى عمولة الغسلة مستقلة عنها.
    - **FAIL:** لا تحفظ الحركة، تتكرر، أو يتغير سجل الغسيل/العمولة بصورة خاطئة.

16. **اختبر معرضاً وديناً ودفعة.** أنشئ غسلة `WIN-TEST-03` على حساب `WIN-TEST-معرض`، ثم افتح «ديون المعارض» وسجل دفعة جزئية صغيرة.
    - **PASS:** تظهر الغسلة كدين للمعرض، وتقل قيمة الرصيد المتبقي بالدفعة من دون تكرار الغسلة.
    - **FAIL:** لا يظهر الدين، لا تؤثر الدفعة، أو تتغير غسلة/رصيد آخر.

17. **اختبر مصروفاً وحساباته.** من قسم التنفيذ المالي أضف مصروف أعمال صغيراً بملاحظة `WIN-TEST`، ثم افتح التقارير المالية لنفس التاريخ.
    - **PASS:** يظهر المصروف مرة واحدة ويتغير صافي الربح/توزيع المصروف وفق القيم المعروضة في النظام.
    - **FAIL:** لا يظهر المصروف، يتكرر، أو تصبح الإجماليات غير متسقة.

18. **راجع التقارير المهمة.** حدّد فترة تشمل اليوم ثم افتح: «التقارير المالية» (التشغيلية والمالية للمدير) و«ديون المعارض» للمعرض الاختباري.
    - **PASS:** تتضمن التقارير بيانات `WIN-TEST-` الصحيحة ضمن الفترة ولا تعرض بيانات خارج الفترة.
    - **FAIL:** تقرير فارغ رغم وجود البيانات، أو إجماليات/تواريخ/بيانات غير صحيحة.

19. **اطبع تقرير A4 إلى PDF.** من ملف دين المعرض اختر «طباعة تقرير الدين»، واختر Microsoft Print to PDF وورق A4؛ اختبر تقريراً يحتوي عدة صفوف إن أمكن.
    - **PASS:** ملف PDF يفتح، النص العربي RTL واضح، لا توجد عناصر واجهة زائدة، ولا قص أفقي/عمودي؛ رأس الجدول يظهر في الصفحات التالية وتبقى الصفوف الطويلة مقروءة.
    - **FAIL:** قص أو تداخل أو صفحات فارغة غير مبررة أو خط عربي مفقود أو أعمدة خارج الورقة.

20. **أنشئ نسخة احتياطية.** كمدير افتح «النسخ الاحتياطي والاستعادة» واضغط «إنشاء نسخة احتياطية»، واحفظ ملف `.db` الناتج في مجلد تعرفه، مثل Documents.
    - **PASS:** تظهر رسالة نجاح، يظهر السجل في قائمة النسخ، وملف `.db` موجود في المكان الذي اخترته.
    - **FAIL:** لا ينشأ الملف أو تكون رسالته خطأ أو لا يمكن العثور عليه.

21. **عدّل بيانات الاختبار بعد النسخة.** أضف غسلة جديدة باسم `WIN-TEST-AFTER-BACKUP` أو عدّل ملاحظة اختبار واضحة.
    - **PASS:** يظهر التعديل بوضوح قبل الاستعادة.
    - **FAIL:** لا يحفظ التعديل أو لا يمكن تمييزه عن بيانات النسخة.

22. **استعد النسخة.** اختر «اختيار ملف للاستعادة»، حدد ملف `.db` من الخطوة 20، واقبل التحذير.
    - **PASS:** تظهر رسالة نجاح وتطلب منك تسجيل الدخول مجدداً.
    - **FAIL:** يقبل ملفاً تالفاً، يتوقف التطبيق، أو لا تظهر نتيجة واضحة.

23. **تحقق من الاستعادة.** ادخل كمدير وافتح السجلات التي فحصتها.
    - **PASS:** تعود البيانات إلى حالة الخطوة 20؛ يظهر ما كان قبل النسخة ويختفي التعديل `WIN-TEST-AFTER-BACKUP`.
    - **FAIL:** تبقى البيانات اللاحقة للنسخة، أو تختفي بيانات كانت داخل النسخة.

24. **أغلق وافتح بعد الاستعادة.** أغلق ERP كلياً ثم افتحه وسجّل الدخول.
    - **PASS:** تبقى الحالة المستعادة كما هي ويعمل التطبيق بصورة طبيعية.
    - **FAIL:** تلف قاعدة البيانات، عودة إعداد أول تشغيل، أو فقدان البيانات المستعادة.

25. **ألغِ تثبيت ERP.** من Windows Settings > Apps > Installed apps اختر مركز الكحيلي لغسيل السيارات ثم Uninstall.
    - **PASS:** تزال ملفات التطبيق والاختصارات ويختفي من قائمة التطبيقات من دون خطأ.
    - **FAIL:** يتوقف إلغاء التثبيت، أو يبقى البرنامج قابلاً للتشغيل من قائمة Start، أو تظهر رسالة فشل.

26. **تحقق من سلوك البيانات بعد الإلغاء.** التثبيت الحالي مصمم للإبقاء على بيانات المستخدم في `%LOCALAPPDATA%\ly.alkaheli.carwash\AlkaheliCarWashERP` عند التحديث أو الإزالة. أعد تثبيت نفس المثبت ثم افتحه.
    - **PASS:** لا يبقى البرنامج نفسه بعد الإزالة، لكن بعد إعادة التثبيت تعود بيانات `WIN-TEST-` المستعادة من النسخة؛ هذا هو السلوك المقصود لحماية البيانات.
    - **FAIL:** يحذف الإلغاء بيانات AppData رغم التصميم، أو لا يعيد التثبيت تشغيل التطبيق، أو لا تتطابق البيانات مع الحالة المستعادة.

**نتيجة الإصدار:** لا يُعتبر الإصدار جاهزاً للإنتاج إلا إذا كانت كل الخطوات PASS على جهاز Windows نظيف وبالمثبت المبني من المصدر الحالي.
